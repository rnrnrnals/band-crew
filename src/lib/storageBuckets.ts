/** Supabase Storage bucket for all BandCrew media (Step E). */
export const STORAGE_BUCKET = 'media';

export type MediaFolder = 'posts' | 'stories' | 'audio' | 'chat' | 'profiles' | 'teams' | 'practice';
